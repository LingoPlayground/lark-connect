{
  "name": "curiosea-lark-connect",
  "version": "0.1.0",
  "description": "Connect local coding agents to Feishu/Lark group feedback loops.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "curiosea-lark-connect": "src/cli.js"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test",
    "typecheck": "npm run build"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.66.1"
  },
  "overrides": {
    "axios": "^1.17.0"
  },
  "engines": {
    "node": ">=22"
  }
}
